spring:
  datasource:
    url: jdbc:mysql://localhost:3306/carepulse_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 55720jnko
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        type:
          preferred_uuid_jdbc_type: VARCHAR

logging:
  level:
    com.statelycare.erp: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
